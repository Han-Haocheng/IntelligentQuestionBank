<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qbank.mapper.AiAnalysisMapper">

    <select id="selectPage" resultType="com.qbank.entity.AiAnalysis">
        SELECT * FROM ai_analysis WHERE user_id = #{userId} ORDER BY id DESC
    </select>

    <select id="countByUser" resultType="int">
        SELECT COUNT(*) FROM ai_analysis WHERE user_id = #{userId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_analysis (user_id, question_id, type, content, model)
        VALUES (#{userId}, #{questionId}, #{type}, #{content}, #{model})
    </insert>
</mapper>
