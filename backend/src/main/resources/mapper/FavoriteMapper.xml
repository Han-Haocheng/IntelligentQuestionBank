<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qbank.mapper.FavoriteMapper">

    <select id="find" resultType="com.qbank.entity.Favorite">
        SELECT * FROM favorite WHERE user_id = #{userId} AND question_id = #{questionId}
    </select>

    <select id="selectPage" resultType="com.qbank.entity.Question">
        SELECT q.*, c.name AS category_name
        FROM favorite f
        INNER JOIN question q ON q.id = f.question_id
        LEFT JOIN category c ON c.id = q.category_id
        WHERE f.user_id = #{userId}
        ORDER BY f.id DESC
    </select>

    <select id="countByUser" resultType="int">
        SELECT COUNT(*) FROM favorite
        <where><if test="userId != null">user_id = #{userId}</if></where>
    </select>

    <select id="selectIdsByUserAndQuestionIds" resultType="long">
        SELECT question_id FROM favorite
        WHERE user_id = #{userId} AND question_id IN
        <foreach collection="questionIds" item="qid" open="(" separator="," close=")">#{qid}</foreach>
    </select>

    <insert id="insert">
        INSERT INTO favorite (user_id, question_id) VALUES (#{userId}, #{questionId})
    </insert>

    <delete id="delete">
        DELETE FROM favorite WHERE user_id = #{userId} AND question_id = #{questionId}
    </delete>

    <delete id="deleteByQuestionIds">
        DELETE FROM favorite WHERE question_id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
    </delete>
</mapper>
