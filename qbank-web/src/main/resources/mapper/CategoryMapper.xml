<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qbank.mapper.CategoryMapper">

    <select id="findById" resultType="com.qbank.entity.Category">
        SELECT * FROM category WHERE id = #{id}
    </select>

    <select id="selectAll" resultType="com.qbank.entity.Category">
        SELECT * FROM category ORDER BY sort, id
    </select>

    <select id="selectChildren" resultType="com.qbank.entity.Category">
        SELECT * FROM category WHERE parent_id = #{parentId} ORDER BY sort, id
    </select>

    <select id="countChildren" resultType="int">
        SELECT COUNT(*) FROM category WHERE parent_id = #{parentId}
    </select>

    <select id="countQuestions" resultType="int">
        SELECT COUNT(*) FROM question WHERE category_id = #{categoryId}
    </select>

    <select id="countAll" resultType="int">
        SELECT COUNT(*) FROM category
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO category (name, parent_id, sort, user_id)
        VALUES (#{name}, #{parentId}, #{sort}, #{userId})
    </insert>

    <update id="update">
        UPDATE category
        <set>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="sort != null">sort = #{sort},</if>
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateSort">
        UPDATE category SET sort = #{sort} WHERE id = #{id}
    </update>

    <select id="countQuestionsInSubtree" resultType="int">
        SELECT COUNT(*) FROM question
        WHERE category_id = #{categoryId}
           OR category_id IN (SELECT id FROM category WHERE parent_id = #{categoryId})
    </select>

    <update id="moveQuestions">
        UPDATE question SET category_id = #{toId}
        WHERE category_id = #{fromId}
           OR category_id IN (SELECT id FROM category WHERE parent_id = #{fromId})
    </update>

    <delete id="deleteById">
        DELETE FROM category WHERE id = #{id}
    </delete>
</mapper>
